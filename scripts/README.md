# Scripts

## Planned Scripts

- `scaffold.sh <theme>`: Run scaffold agent, create app in `apps/<name>`
- `test.sh <app-path>`: Boot sim, install app, run Maestro flows
- `deploy.sh <app-path>`: EAS build + submit to TestFlight
- `run-factory.sh`: Full loop (idea → deploy)
