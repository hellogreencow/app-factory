# Config

Configuration templates and defaults.

## Planned Files

- `eas.json`: EAS Build config
- `maestro.yaml`: Maestro defaults (appId, device)
- `env.example`: Required env vars (API keys, Apple credentials)
